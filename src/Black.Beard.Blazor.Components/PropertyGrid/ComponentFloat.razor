@inherits ComponentFieldBase<float>
    @using MudBlazor

    <MudNumericField 
        T="float"
        @bind-Value="Value"
        Label="@Property.GetDisplay()"
        HelperText="@Property.GetDescription()"
        Disabled="@Property.ReadOnly"
        Validation="Validate"
        Format="@Property.FormatString"
        Variant="@CurrentVariant"
        Margin="@CurrentMargin"
        Min="@GetMinimum()"
        Max="@GetMaximum()"
        Step="@GetStep()" />

