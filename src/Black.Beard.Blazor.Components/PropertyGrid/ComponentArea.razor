@inherits ComponentFieldBase<string>
@using MudBlazor

@* <textarea maxlength="@Maximum"
          disabled="@ReadOnly"
          rows="5"
          cols="60"
          onchange="@((ChangeEventArgs e) => HandleOnChange(e))"
          onfocus="@OnFocus">
    @Value
        </textarea>
 *@

<MudTextField Class="fontPropertyGrid"
              T="string"
              Label="@GetDisplay()"
              HelperText="@GetDescription()"
              @bind-Value="Value"
              Validation="Validate"
              Disabled="@ReadOnly"
              Variant="@CurrentVariant"
              Margin="@CurrentMargin"
              onfocus="@OnFocus" />


