@page "/DynamicPropertyComponent"

@if (Descriptor != null && Descriptor.Enabled && Descriptor.ComponentView != null)
{
    <DynamicComponent 
        @ref="Ui" 
        Type="@Descriptor.ComponentView" 
        Parameters="@Parameters" 
        />
}
